<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="assets/styles.css" />
    {% if title == "Home" %}
      <title>Jonathan Christian</title>
    {% else %}
      <title> {{title }} | Jonathan Christian</title>
    {% endif %}
  </head>
  <body>
    <header>
      <h1>Jonathan Christian</h1>
      <nav>
        <ul>
          <li><a href="index.html">Home</a></li>
          <li><a href="now.html">Now</a></li>
          <li><a href="projects.html">Projects</a></li>
          <li><a href="blog.html">Blog</a></li>
        </ul>
      </nav>
    </header>
    <main>
      {% unless title == "Home" or title == "Blog" %}
        <h1>{{ title }}</h1>
      {% endunless %}

      {% if date %}
        <h3>{{ date }}</h3>
      {% endif %}
      
      {{ content }}
    </main>
    <footer>
      <p><a href="#">Subscribe via email</a> for new post notifications.</p>
      <p class="copyright">&copy; {{ "now" | date: "%Y" }} Jonathan Christian.</p>
    </footer>
  </body>
</html> 